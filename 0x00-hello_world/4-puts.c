#include <stdio.h>

/**
 * main - prints the string "Programming is like building a multilingual puzzle
 * return: 0 if executed, do not return zero otherwise
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
